<% if signed_in? %>
  <%= render 'shared/signedin_home' %>
<% else %>
	<div class="center">
	  <h1>Want to meet friends and discover around the world</h1>
	  <%= render 'shared/search_earth' %>
	  <%= link_to "Join WorldVille!", signup_path, class: "btn btn-large btn-primary" %>
	</div>
<% end %> 

